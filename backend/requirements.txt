# =============================================================================
# Oil Spill Detection & Vessel Attribution System — Backend Dependencies
# SIH PS #26143
# =============================================================================

# --- Core ML / Detection (Module A) ---
torch>=2.0.0
torchvision>=0.15.0
opencv-python-headless>=4.8.0
scikit-learn>=1.3.0
scikit-image>=0.21.0

# --- Geospatial Processing ---
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
pyproj>=3.6.0

# --- Drift Simulation (Module B) ---
# NOTE: OpenDrift + GDAL are installed via conda in production environments.
# For the Docker demo, we use pre-cached synthetic drift data instead.
# opendrift>=1.11.0
xarray>=2023.6.0
netCDF4>=1.6.0
scipy>=1.11.0

# --- Environmental Data Access ---
copernicusmarine>=1.0.0        # CMEMS Python toolbox
cdsapi>=0.6.0                  # Copernicus Climate Data Store (ERA5)

# --- API / Backend (FastAPI) ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6        # File upload support
pydantic>=2.5.0

# --- Report Generation ---
reportlab>=4.0.0               # PDF generation
jinja2>=3.1.0                  # HTML templating for reports

# --- Data Handling ---
pandas>=2.1.0
numpy>=1.24.0
matplotlib>=3.8.0
h5py>=3.10.0

# --- Utilities ---
tqdm>=4.66.0
python-dotenv>=1.0.0           # .env file for credentials
loguru>=0.7.0                  # Structured logging
geojson>=3.1.0
duckdb>=1.0.0                  # In-process SQL analytics on AIS Parquet files
